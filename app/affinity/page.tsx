import type { Metadata } from "next";
import AffinityApp from "./AffinityApp";

export const metadata: Metadata = { title:"合缘观照｜观象", description:"以确定性四柱与关系结构，看见两个人的同频、互补与沟通边界。" };
export default function AffinityPage() { return <AffinityApp />; }
